TODO LIST:
- a page to introduce basic of twig
- application management through XMPP bot
- auto-remove user info when user are no longer bound with any account
- more-friendly output (according to XMPP source)
- need a strategy for id-shorten
- add prefix before command in help pages
- bot's status updating showing active user's screen_name ( and re-sent presence after user switch )
- bug: outbox of directmessage, recipent's name should be shown rather than sender's
- need a document for how to register twitter app, including KEY, SECRET and CALLBACK
